//Lowercase to uppercase without using function in programming in c.

#include<stdio.h>
int main()
{
    char lower;
    printf("Enter lowercase latter = ");
    scanf("%c",&lower);
    printf("Uppercase latter is = %c",lower-32);
    return 0;
}


