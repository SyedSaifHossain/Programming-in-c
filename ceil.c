//The value of Function of ceil in programming in c.

#include<stdio.h>
#include<math.h>
int main()
{
    double x,result;
    printf("Enter the value of x = ");
    scanf("%lf",&x);
    result=ceil(x);
    printf("The value of function of ceil(%lf) = %lf",x,result);
    return 0;
}

